spring:
  datasource:
    url: jdbc:h2:mem:couponservice
    username: sa
    password:
    driver-class-name: org.h2.Driver
#  h2:
#    console:
#      enabled: true
#      path: /h2
  flyway:
    enabled: false
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true